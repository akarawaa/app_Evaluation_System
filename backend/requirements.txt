# FastAPI stack
fastapi
uvicorn[standard]
pydantic
pydantic-settings

# DB access (primary): SQLAlchemy async + asyncpg
sqlalchemy[asyncio]
asyncpg
# On Python 3.9 without a C compiler, the latest greenlet has no cp39 wheel and
# fails to build. Pin to a version that ships one:
greenlet==3.1.1 ; python_version < "3.10"

# Supabase (used for Auth admin tasks only, e.g. inviting/creating users)
supabase

# Auth / JWT verification
pyjwt[crypto]
python-jose[cryptography]

# Logging
structlog

# Reporting: PDF generation (Phase 3)
reportlab

# Dev / test
pytest
pytest-asyncio
httpx
pip-audit
ruff
